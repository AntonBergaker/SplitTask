<UserControl x:Class="TaskPlanner.TaskTreeNode"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskPlanner"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" Height="28.479">
    <Grid x:Name="grid">
        <Rectangle x:Name="background"/>
        <Grid x:Name="offsetGrid" Margin="0,0,0,0">
            <CheckBox x:Name="checkBox" Content="" HorizontalAlignment="Left" Margin="25,5,0,0" VerticalAlignment="Top"/>
            <TextBox x:Name="textBox" Height="23" Margin="50,3,20,0" TextWrapping="Wrap" Text="TextBox" HorizontalAlignment="Left" VerticalAlignment="Top" BorderThickness="0"/>
        </Grid>
    </Grid>
</UserControl>
